using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StationaryRangedEnemy : BaseEnemy
{
    // Start is called before the first frame update
    void Start()
    {
        OnStart();
    }
}
